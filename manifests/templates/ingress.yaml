---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: javaproject                  # Ingress name
  labels:
    app: javaproject
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "javaproject.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: javaproject               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
